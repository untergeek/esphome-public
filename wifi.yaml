---
defaults:
  wifi_ssid: NOT_CONFIGURED
  wifi_pass: NOT_CONFIGURED
  wifi_domain: example.org
  fast_connect: on  
  
wifi:
  ssid: ${wifi_ssid}
  password: ${wifi_pass}
  domain: ${wifi_domain}
  fast_connect: ${fast_connect}
  use_address: ${ipaddr}

  ap:
    ssid: AP_${devicename}

